/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */

import React, {
  AppRegistry
} from 'react-native';

import AppContainer from './app/containers';

AppRegistry.registerComponent('reactTest', () => AppContainer);
